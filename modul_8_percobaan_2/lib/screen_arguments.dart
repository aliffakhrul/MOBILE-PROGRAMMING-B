class ScreenArguments {
  final String cover;
  final String title;
  final String shortDesc;
  final String description;

  ScreenArguments(this.cover, this.title, this.shortDesc, this.description);
}